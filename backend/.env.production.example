# Example production environment variables for backend
# Copy to backend/.env and fill values before starting the server

PORT=5000
MONGO_URI=mongodb://localhost:27017/5m-prod
JWT_SECRET=change_this_secret
NODE_ENV=production
