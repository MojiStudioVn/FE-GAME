{
  "watch": ["server"],
  "ext": "ts,json",
  "ignore": ["server/**/*.spec.ts", "server/**/*.test.ts"],
  "exec": "ts-node -P tsconfig.server.json server/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
